{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ formation.datedebut ? formation.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ formation.datefin ? formation.datefin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formationn_index') }}">back to list</a>

    <a href="{{ path('formationn_edit', {'id': formation.id}) }}">edit</a>

    {{ include('formationn/_delete_form.html.twig') }}
{% endblock %}
